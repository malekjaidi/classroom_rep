{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
 {#{{ form_start(registrationForm) }}
   
        {{ form_row(registrationForm.username) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.roles) }}

        {{ form_row(registrationForm.agreeTerms) }}
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

        <button type="submit" class="btn">Register</button>
        {{ form_end(registrationForm) }}#}






 {{ form_start(registrationForm) }}
  
<section class="vh-100" style="background-color: #f5a425;">
<form method="post">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col col-xl-10">
        <div class="card" style="border-radius: 1rem;">
          <div class="row g-0">
            <div class="col-md-6 col-lg-5 d-none d-md-block">
              <img
                src="https://image.freepik.com/free-photo/student-browsing-online-commerce-information-using-laptop-computer-while-sitting-comfortable-sofa-living-room-woman-studying-marketing-course-using-e-learning-university-platform_482257-17455.jpg"
                alt="login form"
                class="img-fluid" style="border-radius: 1rem 0 0 1rem;"
              />
            </div>
            <div class="col-md-6 col-lg-7 d-flex align-items-center">
              <div class="card-body p-4 p-lg-5 text-black">

                <form>

                  {#<div class="d-flex align-items-center mb-3 pb-1">
                    <i class="fas fa-cubes fa-2x me-3" style="color: #ff6219;"></i>
                    <span class="h1 fw-bold mb-0">Logo</span>
                  </div>#}

                  <h5 class="fw-normal mb-3 pb-3" style="letter-spacing: 1px;">Créer votre compte</h5>

                  <div class="form-outline mb-4">
                  <label class="form-label" for="form2Example17" for="inputUsername">Nom d'utilisateur</label>
                  {{ form_row(registrationForm.username, {label: ' '}) }}
                  </div>

                  <div class="form-outline mb-4">
                 <label class="form-label" for="form2Example17" for="inputUsername">Mot de passe</label>
                  {{ form_row(registrationForm.plainPassword, {label: ' '}) 
                  }}
                  </div>

                  <div class="form-outline mb-4">
                 <label class="form-label" for="form2Example17" for="inputUsername">Role</label>
                  {{ form_row(registrationForm.roles, {label: ' '}) }}

                  </div>

                  <div class="form-outline mb-4">
                 <label class="form-label" for="form2Example17" for="inputUsername"></label>

                  {{ form_row(registrationForm.agreeTerms, {label: 'Accepter les conditions d\'utilisation'}) }}
                  </div>

                  <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                  <div class="pt-1 mb-4">
                    <button class="btn btn-warning" type="submit">S'inscrire</button>
                  </div>
                  {{ form_end(registrationForm) }}
                  {#<a class="small text-muted" href="#!">Forgot password?</a>#}
                  <p class="mb-5 pb-lg-2" style="color: #393f81;">Vous avez déjà un compte ? <a href="/login" style="color: #393f81;">Se connecter içi</a></p>
                  <!--<a href="#!" class="small text-muted">Terms of use.</a>
                  <a href="#!" class="small text-muted">Privacy policy</a>-->

              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</form>  
</section>
 

{% endblock %}
