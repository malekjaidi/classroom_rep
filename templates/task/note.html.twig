{% extends 'base.html.twig' %}

{% block title %}Note{% endblock %}

{% block body %}
<div class="container">
{{ form_start(form) }}
    {{ form_widget(form) }} 
    <br>
    <center><button  class="btn btn-outline-success">{{ button_label|default('Save') }}</button></center>
{{ form_end(form) }}
</div>
{% endblock %}